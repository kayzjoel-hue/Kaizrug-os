# Ecosystem Organization Notes

## 2026-05-21 - Kaizrug OS v1 Showroom

- Preserved active public pages: `index.html`, `templates/studio.html`, and
  `docs/workshop.html`.
- Kept `css/main.css` active because all public pages depend on it.
- Moved unused placeholder assets and old helper files to `archive/legacy/`.
- Moved private Stripe/business review notes to ignored `private/` storage.
- Added a safe public `README.md` and `.gitignore`.
